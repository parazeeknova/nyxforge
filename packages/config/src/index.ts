// Export Next.js configuration
export { default as createNextConfig } from "./next";

// Export TypeScript configurations via JSON imports
// These will be referenced directly in tsconfig.json files
